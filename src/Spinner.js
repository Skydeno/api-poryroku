import React from "react";

const Spinner = () => {
    return (
        <div className="ui active dimmer">
            <div class="ui massive text loader">Ładowanie danych</div>
        </div>
    );
}

export default Spinner; 